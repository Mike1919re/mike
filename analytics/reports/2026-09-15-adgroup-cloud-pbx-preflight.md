# Pre-flight · קבוצת מודעות "מרכזיה בענן" — לפני הפעלה

**מזהה:** `70525416466` · קמפיין `1806300176` · סטטוס נוכחי: **PAUSED** · נבדק 15/09/2026

---

## ✅ בדיקת ה-Broad — נקי

| | |
|---|---|
| מילות מפתח בקבוצה (כל הסטטוסים) | 61 |
| **ENABLED** | **13** |
| REMOVED | 48 |
| שלילות ברמת הקבוצה | 112 |

### 13 המילים הפעילות

| התאמה | סטטוס הגשה | מילה |
|---|---|---|
| EXACT | ELIGIBLE | `מרכזיה בענן` |
| EXACT | ELIGIBLE | **`מרכזיה בענן מחיר`** |
| EXACT | ELIGIBLE | `מרכזיה בענן לעסקים` |
| EXACT | ELIGIBLE | `מרכזיות בענן` |
| EXACT | ELIGIBLE | `מרכזיות ענן` |
| EXACT | ELIGIBLE | `מרכזיות טלפון בענן` |
| EXACT | ELIGIBLE | `מרכזיות ip בענן` |
| EXACT | ELIGIBLE | `מרכזייה בענן` |
| EXACT | ELIGIBLE | `מרכזיית ענן` |
| EXACT | ELIGIBLE | `מרכזיית ip בענן` |
| EXACT | ELIGIBLE | `שירות מרכזיה בענן` |
| PHRASE | ELIGIBLE | `מרכזיית טלפון בענן` |
| PHRASE | ELIGIBLE | `מרכזיית טלפוניה בענן` |

> ## **BROAD בקבוצה: 0**
> ## **Low Search Volume: 0 — כל 13 במצב `ELIGIBLE`**

11 EXACT · 2 PHRASE. כל וריאנטי הכתיב מכוסים (`מרכזיה` / `מרכזייה` / `מרכזיית`).
**`מרכזיה בענן מחיר` — המילה בעלת הכוונה המסחרית הגבוהה ביותר מרובד 1 — כבר קיימת.**

---

## ✅ היסטוריית ביצועים — 12 חודשים אחרונים

```
חשיפות 0 | קליקים 0 | ₪0.00 | המרות 0
```

**הקבוצה לא הגישה כלל בשנה האחרונה.** זה סוגר את הדגל שהעליתי: היא לא הושהתה בגלל
CPC גבוה או ביצועים גרועים בתקופה הנמדדת. **אין היסטוריה שלילית שמצדיקה זהירות.**

> **הסתייגות:** ייתכן שהיא הושהתה לפני יותר משנה. חלון הנתונים אינו מגיע לשם.

---

## 🚫 ההפעלה עצמה — לא ניתנת לביצוע מכאן

הסביבה חוסמת במפורש את כלי הכתיבה לחשבון:

| כלי | נדרש עבור | סטטוס |
|---|---|---|
| `GOOGLEADS_MUTATE_AD_GROUPS` | הפעלת הקבוצה (סעיף 4) | 🚫 **מוגבל** |
| `GOOGLEADS_MUTATE_AD_GROUP_CRITERIA` | הוספת `בזק` כשלילה (סעיף 3) | 🚫 **מוגבל** |
| `GOOGLEADS_MUTATE_CAMPAIGNS` | מעבר ל-Maximize Clicks (סעיף 2) | 🚫 **מוגבל** |

**הרשאת הקריאה מלאה; הכתיבה חסומה במדיניות הסביבה.** שלושת השינויים ידניים.

**נתיב הקליקים:**
`Campaigns → מרכזיות טלפון תנועה לאתר → Ad groups → מרכזיה בענן → Status → Enabled`

---

## ⚠️ שתי הערות לפני שתבצע

### א. `בזק` כשלילה — נכון ברמת הקבוצה, **מסוכן ברמת הקמפיין**

הוספה ל-`קו תמסורת` בלבד — **מדויק ונכון.** עוצר את שריפת התקציב על `בזק עסקים אינטרנט`.

**אבל אל תקדם אותה לרמת הקמפיין לעולם.** היא תהרוס:

| מה ייחסם | נפח |
|---|---|
| `סנטריקס בזק` | 392 חשיפות אורגניות · מיקום 7.6 |
| קבוצת `מרכזיות מתחרים בזק פרטנר סלקום` | **178 מילות מפתח** |
| `מרכזיות בזק לעסקים` · `מרכזיה בזק` | כיבוש מתחרים לגיטימי |

יש בחשבון קבוצת מודעות שנבנתה **במיוחד** לכיבוש שאילתות בזק. שלילה ברמת קמפיין
תסתור אותה ישירות.

### ב. תקרת CPC של ₪18 — מתחת למחיר השוק שנצפה

**הקליק היחיד שנמדד בקמפיין עלה ₪28.89.**

תקרה של ₪18 היא בקרת עלות לגיטימית, אבל שים לב: אם מחיר השוק בפועל הוא כ-₪29,
תקרה של ₪18 **תדכא הגשה** — ותשחזר את אותם 15 חשיפות, הפעם מסיבה אחרת.

**הצעה:** להתחיל ב-₪18 כפי שתכננת, **ולמדוד `search_top_impression_share` אחרי שבוע.**
אם הוא נשאר מתחת ל-20% — התקרה היא החסם, ולא הביקוש. **מדגם של קליק אחד אינו מחיר
שוק**, ולכן אל תקבע תקרה גבוהה יותר על סמכו מראש.

---

## סדר מומלץ לביצוע היום

| # | פעולה | הערה |
|---|---|---|
| 1 | ייבוא המרות GA4 → Ads כ-Primary | ✅ כפי שתכננת |
| 2 | מיקוד מיקום → **Presence בלבד** | **לפני** שינוי הבידינג |
| 3 | בידינג → Maximize Clicks · תקרה ₪18 | |
| 4 | `בזק` כשלילה — **ברמת `קו תמסורת` בלבד** | |
| 5 | הפעלת `מרכזיה בענן` | אחרון — כדי שתיפתח לתוך הגדרות תקינות |

**Presence לפני הבידינג** — ברגע שהבידינג משתחרר, 19 מילות ה-SIP באנגלית ב-`SIP TRUNK`
מתחילות להגיש. בלי מיקוד נוכחות, הן ימשכו תנועה בינלאומית.
